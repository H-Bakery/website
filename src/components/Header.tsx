import styles from './Header.module.scss';

const Header = () => (
  <div>
    <h3 className={styles.tag}>
      <span>Happy</span> <span>to</span> <span>be</span> <span>here</span>
    </h3>
  </div>
);

export { Header };
