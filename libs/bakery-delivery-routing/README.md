# bakery-delivery-routing

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build bakery-delivery-routing` to build the library.

## Running unit tests

Run `nx test bakery-delivery-routing` to execute the unit tests via [Jest](https://jestjs.io).
