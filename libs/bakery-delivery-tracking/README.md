# bakery-delivery-libs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build bakery-delivery-libs` to build the library.

## Running unit tests

Run `nx test bakery-delivery-libs` to execute the unit tests via [Jest](https://jestjs.io).
