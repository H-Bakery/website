# baking-list

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build baking-list` to build the library.

## Running unit tests

Run `nx test baking-list` to execute the unit tests via [Jest](https://jestjs.io).
