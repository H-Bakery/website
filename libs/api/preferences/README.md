# preferences

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build preferences` to build the library.

## Running unit tests

Run `nx test preferences` to execute the unit tests via [Jest](https://jestjs.io).
