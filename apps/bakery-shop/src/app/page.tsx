'use client'

import React from 'react'
import { StorefrontHome } from '@bakery/shop/feature-catalog'

/** Startseite des Shops: Einstieg in den Katalog, kein Marketing-Auftritt. */
export default function HomePage() {
  return <StorefrontHome />
}
