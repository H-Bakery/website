# Environment Configuration Example
# Copy this file to .env and update with your actual values

# Google API Configuration (optional)
GOOGLE_API_KEY=your-google-api-key-here

# JWT Configuration (REQUIRED)
# SECURITY WARNING: You MUST generate a new secure secret before using this application!
# 
# To generate a secure JWT secret, use one of these commands:
#   Node.js:  node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
#   OpenSSL:  openssl rand -base64 64
#   
# The secret should be at least 32 characters, but 64+ is recommended for production.
# NEVER use the example secret below in production!
JWT_SECRET=CHANGE-THIS-your-very-secure-jwt-secret-key-change-this-in-production-minimum-32-chars

# Environment
NODE_ENV=development
PORT=5000

# Database Configuration (optional for future use)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=bakery_db
# DB_USER=bakery_user
# DB_PASS=secure_password

# Email Configuration (optional)
# EMAIL_PROVIDER=smtp # Options: smtp, gmail, sendgrid, aws-ses
# EMAIL_HOST=smtp.gmail.com # For custom SMTP
# EMAIL_PORT=587
# EMAIL_SECURE=false # true for 465, false for other ports
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password # Use app-specific password for Gmail
# EMAIL_FROM=noreply@bakery.com
# EMAIL_FROM_NAME=Bakery Notifications
# EMAIL_TLS_REJECT_UNAUTHORIZED=true

# Gmail specific (when EMAIL_PROVIDER=gmail)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-specific-password

# SendGrid specific (when EMAIL_PROVIDER=sendgrid)
# SENDGRID_API_KEY=your-sendgrid-api-key

# AWS SES specific (when EMAIL_PROVIDER=aws-ses)
# AWS_SES_ENDPOINT=email-smtp.us-east-1.amazonaws.com
# AWS_SES_USERNAME=your-ses-smtp-username
# AWS_SES_PASSWORD=your-ses-smtp-password

# Email behavior
# SEND_BROADCAST_EMAILS=false # Send emails for broadcast notifications
# APP_URL=http://localhost:3000
# LOGO_URL=https://your-bakery.com/logo.png

# Notification Archival Configuration (optional)
# ARCHIVAL_ENABLED=true # Enable/disable automatic archival service
# ARCHIVAL_DAYS=30 # Archive notifications older than X days
# CLEANUP_DAYS=90 # Permanently delete archived notifications older than X days